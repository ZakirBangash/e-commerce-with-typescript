export interface productInfo {
    category: string
    description: string
    id:number
    image:string
    price:number
    title:string
    quantity? : number
}

export interface title_types {
    name : string
    title : string
}
